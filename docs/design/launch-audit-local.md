# 蛮有味 · 上线门禁自查报告（本地可验证项）

> **生成**：2026-08-15 夜间（W1–W8 + W3.1b 全城坐标后）· **范围**：D1–D10 中不依赖外部资源（服务器/域名/短信/微信/实地）可本地验证的部分；其余标注「待 Robin 补充后验证」。
> 配套：docs/design/go-live-roadmap.md（路线图）、docs/design/dual-track-agent-plan.md（双轨方案）。

---

## 一、验收对照（本地已验证）

| # | 标准 | 状态 | 证据 |
|---|------|------|------|
| D1 | 分享链接可用 | 🟡 本地静态 200 / 生产 URL 待 R2 | 前端 :5180 HTTP 200；verify-loop 全绿 |
| D2 | 核心闭环（问→推演→推荐→导航/收藏） | ✅ 端到端验证 | /run 关键词检索（老樊城→2 家精准）；「清淡的健身餐」→ 双轨自动升级 DeepSeek 返回推荐（15s/8096 tokens）；前端五阶段渐进推演 + 主推卡 + 追问条 |
| D3 | 南湖「就近」可用 | ✅ 145/185（78%） | geocode 补全后实测；**缺口 40 家为「东门」类区域名（高德不可解析），待实地/众包定位** |
| D4 | 本地生活可见（流动摊/上传） | ✅ 代码就绪 | 全城缺坐标 328→82；上传三分支 + 治理（ADMIN_TOKEN）+ 脱敏列表；流动摊 206 家保留（核心区 62 家有坐标） |
| D5 | 注册/登录闭环 | 🟡 代码全通 / 生产短信待 R3 | 图形码→短信码（console dev 模式）→JWT→收藏跨设备；协议必填/昵称/注销（删数据+吊销）/登出吊销 11 项测试 |
| D6 | 商业化迁入福利 Tab | ✅ | 签到/抽奖/任务/券包/演示核销全在 welfare.js；首页无商业化字样 |
| D7 | 数据不丢 | ✅ 文件持久化 + 备份 | auth-users/favorites/merchant-uploads 原子写；**scripts/backup-data.mjs 备份轮转（保留 10 份）**；重启旧 JWT 有效（子进程模拟） |
| D8 | 安全无硬伤 | ✅ 全项通过 | 写操作全 JWT（越权渗透实测：改 userId 被忽略）；CORS 白名单（非白名单 403）；限流（敏感 30rpm/LLM 10rpm/429）；治理接口 401；请求体 1MB；手机号 AES-256-GCM + 哈希索引（加密模式重启测试）；日志脱敏；npm audit 0 漏洞；全仓红线扫描 0 命中 |
| D9 | 性能 | ✅ 拆包达成 | 主包 710KB→41KB（gzip 16.6KB）；数据/地图/账号/Agent 分包；**首屏数据 chunk 73KB gzip 待动态 import 优化（后续）** |
| D10 | 合规 | ✅ 页面就绪 | 隐私政策页（#privacy，协议链接直达）；注册协议必填；verified/estimated 标注（列表/详情徽章）；注销入口 |

---

## 二、待 Robin 补充后验证的项

| 项 | 依赖 | 验证方式 |
|----|------|----------|
| D1 生产 URL | R2 域名+HTTPS | 手机浏览器打开生产链接 |
| D5 真实短信登录 | R3 短信密钥+模板备案 | 真实收码登录（含错误码/频控负路径） |
| D5 微信登录 | R4 微信凭据 | 微信授权跳转回跳 |
| D3 南湖余 40 家 + D4 流动摊定位 | R5 实地探店 / 用户众包上传 | collect-visit.mjs / /upload 流程 |
| 真机走查 | R1 部署后 | iPhone Safari / Android Chrome / 微信内置浏览器 |
| W9 灰度 | 部署 + 用户 | 50–100 人跑 1–2 周，看板核对 |

---

## 三、遗留小项（非阻塞，后续迭代）

1. 首屏动态 import（数据 chunk 懒加载）——当前分块已缓存友好，动态化收益边际。
2. 南湖 40 家「东门」类店：建议众包上传定位（/upload 已就绪 + 多模态识别可辅助）。
3. 短信发送审计日志（console 模式已有 dev 日志；生产 tencent 接入时补结构化审计）。
4. 行为验证码（滑动拼图）作为上线后升级项。

---

*本报告为上线门禁的本地自查快照；外部依赖补齐后按 go-live-roadmap W7→W8→W9 继续。*
