# 蛮有味项目状态总览（2026-08-15 实查）

> 本报告基于实际代码核查（非仅看 roadmap 勾选）。上一份：status-2026-08-13.md。

## 一、总体结论
**S1（文档重构）+ S2（数据统一 V4.4）+ S3（账号持久化）已完成**：SPEC v2.0 按智能体产品标准重构；数据源双端同口径 **860**；账号文件持久化（data/auth-users.json，gitignored、原子写），**重启后旧 JWT 仍有效**（子进程模拟验证）。全量 26 文件 34 子测试全绿；ranking-audit PASS；reconcile unified=true（860 = 860，伪造坐标 0）。

## 二、S1 本轮交付（2026-08-15）
- **docs/SPEC.md v2.0**：新增「用户与场景」「智能体行为规范（对话回路 / 工具规则 / 记忆模型 / 决策契约 / 降级熔断 / 信任内核）」「指标与成功标准（北极星 / 护栏 / 增长）」章节；保留全部既定事实与决策；§11 下一步清单 = S2→S6（S1 已勾选完成）；§12 决策记录入册 D-20260815-01/02；§7 明确推送授权。
- **docs/README.md**：索引更新（v2.0 / status-2026-08-15 / 移除已删 v3.4 占位文档）；守门工作流新增「审阅 → 推送」两步。
- **.context-store/**：CONTROL_TOWER.md 重写（2026-08-15 焦点 + 红线更新）；layers/decisions.md 追加 D-20260815-01/02；layers/open-threads.md 数据口径线程标记已授权（S2 实施中）。
- **docs/status-2026-08-15.md**（本文件）。

## 二点五、S2 数据统一交付（2026-08-15）
- **重名治理（scripts/normalize-data.mjs 新增 resolveDuplicateNames，纯函数 + 单测）**：61 组重名中 58 组为真重复（同址同坐标，合并留首条），3 组为同名不同址分店（**重庆辣子鱼家常菜（恩施街29户25号）/ 阿德鱼湾（二七北路28附16）/ 湖滨客舍（黄鹂路78号…）**——此前被前端静默吞掉，现改名保留，修复真实数据丢失）。
- **后端统一（hypha runtime.js ALL_MERCHANTS = allMerchants；datasource wuhan name → 'wuhan'）**：后端 = 前端 = 860（merchants 567 + robin-99 87 + web-stalls 206），坐标全 null 未伪造（违规 0）。
- **守卫**：reconcile-datasource.mjs 改为「统一后守卫」（对比前端 allMerchants vs 后端运行时 ALL_MERCHANTS + 原始表质量），基线测试锁定 860/860/567/重名 0；vite build 通过（50 模块）。

## 二点六、S3 账号持久化交付（2026-08-15）
- **auth-server.js**：账号（users / phoneIndex / unionIndex）落盘 data/auth-users.json（与 merchant-uploads.json 同目录，已被 .gitignore 覆盖；原子写 tmp+rename；磁盘失败降级为内存运行不阻断登录）；验证码 / 频控保持内存态（短时效安全语义，重启清空属正确行为）。
- **补齐后端账号单测**：新增 hypha/implementation/test/auth.test.mjs（5 组：图形码一次性/短信频控/登录 JWT+脱敏/持久化文件存在/**子进程模拟重启：旧 JWT 仍有效 + 同号重登同一账号**）——此前 status 所称「13/13 进程内单测」从未入库，本次补齐并加固。
- 数据文件未入库（gitignored），红线零违规。

## 三、全量验证实测（2026-08-15，可重跑）
| 项 | 结果 |
|----|------|
| 测试 | **26 文件 34 子测试全绿**（h5 13 / hypha 9 / 集成 1 / scripts 3：新增 normalize-data.test.mjs + auth.test.mjs） |
| node --check | 全部 JS 通过，**唯一例外：scripts/build-robin-99.mjs:94 截断（历史一次性脚本，既有缺陷，其产物 robin-99.mjs 87 条已存在，不影响数据）** |
| ranking-audit | verdict=PASS / prohibitedHits=0 / firewall=3/3 |
| reconcile | **unified=true**：frontend 860 = backend 860 / rawBackend 567 / 重名 0 / 独有 0 / 伪造坐标 0 |
| vite build | 通过（50 模块，dist 522KB，chunk 警告为既有） |
| git | 审阅后提交 + 推送 origin master（D-20260815-01 授权） |

## 四、版本路线图状态（诚实口径）
| 版本 | 状态 | 关键说明 |
|---|---|---|
| V1 LLM 基座 | ✅ 代码 + 真跑 | .env 有 DEEPSEEK_API_KEY，4/4 /agent 真模型（fallback=false）；盲评 ~4.0/5 已修复 |
| V2 信任内核 | ✅ 全交付 | V2.1–2.4 + ranking-audit PASS |
| V3 增长+账号/券 | ✅ 全交付 | V3.1–3.4（券/核销为前端原型） |
| V4 BFF 后端（收窄） | 🟡 实施中 | 账号子系统 ✅（图形+短信+JWT+微信，13/13 单测）；**S2 数据统一已授权待实施**；券/核销/支付延后 |
| V5 规模化/出海 | ⛔ 未启动 | 依赖 V4 |

## 五、下一步（SPEC §11，按序推进）
- **S2 数据统一 V4.4【✅ 已完成 2026-08-15】**：双端 860 同口径、重名 0、Agent 同源。
- **S3 账号持久化【✅ 已完成 2026-08-15】**：文件持久化 + 重启验证 + 后端账号单测补齐。
- **S4 收藏跨设备同步（user.favorite 后端化，JWT 解析防越权）**；S5 pending 上传治理；S6 对话体验打磨（chat-first）。
- 后置：短信网关生产化 / 微信凭据 / V4.1 券核销 BFF / V4.2 商户入驻 / V4.3 分润对账 / 增长实验 / 小程序壳。

## 六、风险（详见 SPEC §13）
R1 账号内存存储（S3 治理中）· R2 核销仅本地（等商家载体）· R3 数据完整度（583 estimated 待探店）· R4 0 商户绑券 · R8 短信网关未生产化 · R9 微信未配置。

---

*生成时间：2026-08-15 · 核查方式：node --test 全量实跑 + node --check + 审计脚本重跑 + git 状态。*
