# 蛮有味项目状态总览（2026-08-13 实查）

> 本报告基于实际代码核查（非仅看 roadmap 勾选），由迭代管家生成。

## 一、总体结论
代码地基（数据层 + 算法层 + 前端 + 本地 Agent 运行时）已完整，**单测全绿（h5 13 + hypha 40：upload 22 / agent-client 18）**。**V1–V3 的代码级任务已全部真实落地并测试通过**；**V1 真跑门禁已解锁且已实跑**（`.env` 含 `DEEPSEEK_API_KEY`，4/4 `/agent` 真模型通过）；**§7.4 探店采集已实施并在真实高德 Key 下三分支验收通过**；**§7.4.1 账号登录体系已实施**（图形+短信+JWT+微信，13/13 单测）。早先的探店采集/入口迁移/archive 删除已 commit+push；**本轮账号体系改动待 commit（未碰红线）**。

## 二、各模块功能完整度

### 1. 数据层（✅ 完成）
- `merchants.js` = 625 商户（自动生成、禁手改）；`all-merchants.js` = 857（含 robin-99 87 + web-stalls 206，坐标全 null 未伪造）。
- 置信度：41 verified + 1 partial + 583 estimated；后端内 **61 组重名被合并去重吞掉**（数据质量隐患，待治理）。

### 2. 算法层（✅ 完成）
- 确定性 `/run`（0 token）+ LLM `/agent` 骨架（:8799）；逐店理由 `explain.js` + 推理时间线。

### 3. 前端 h5（✅ 核心闭环完成）
- 主流程：列表 → 详情 → 领券 → 券包/收藏 → 到店核销 CPS，全通且有测试。
- 信任内核 UI：因子权重可视化、真实性徽章、反广告审计、探店采集工具，均真实实现。
- 增长：校园增长看板 + 文案/二维码占位（域名待定）。
- 地图/榜单/搜索/口味等模块齐全。

### 4. 本地 Agent 运行时 hypha（✅ 完成，真跑已验）
- 10 工具 + 数据源抽象 + DeepSeek 客户端（真实 `fetch` + tool_calling）+ ReAct + httpServer 降级熔断。
- **真跑验收（本轮）**：4 个探店问题实跑 `/agent` 全部 `fallback=false`（真 LLM）；3 个盲评智能体一致 ~4.0/5，共识 4 项硬伤已修复（提示词新增「体验纪律」）。

### 4.1 V4 后端·账号登录体系（✅ 本轮实施）
- 后端 `:8799`：图形验证码（自绘 SVG）/ 短信验证码（频控+一次性+常量时间）/ JWT（HMAC-SHA256 内联）/ 微信网页授权；6 端点全通，进程内单测 13/13 全绿，负路径实测 200/400/401 正确。
- 前端 h5：`LoginView`（手机+图形+短信+微信）+ `auth-client` + `applyRemoteSession` 桥接 + 微信 callback 落地 + 登录样式；`vite build` 50 模块通过。

## 三、路线图任务完成度

| 版本 | 状态 | 关键说明 |
|---|---|---|
| V1 LLM 基座 | ✅ 真跑已执行 | **在 `.env` 跑通 `DEEPSEEK_API_KEY` 真模型**：4 个探店问题实测 `/agent` 全部 `fallback=false`（真 LLM 驱动）；3 个盲评智能体一致给 ~4.0/5，已据此修复提示词（新增「体验纪律」：距离一致明示/必给可执行出口/禁绝对化用词/禁凑无关项）。累计成本/延迟待长周期统计。 |
| V2 信任内核 | ✅ 全交付 | V2.1–2.4 真实落地 + 测试 |
| V3 增长+账号/券 | ✅ 全交付 | V3.1–3.4 真实落地 + 测试 |
| V4 BFF 后端 | 🟢 账号子系统已实施 | **账号登录体系已落地**（§7.4.1）：图形验证码+短信验证码+JWT+微信授权，后端 `:8799` 零依赖 + 前端 h5 全链路；数据治理 V4.4 仍待授权。 |
| V5 规模化/出海 | ⛔ 未启动 | 依赖 V4 |
| §7.4 探店采集 | ✅ 已实施+真跑验收 | 后端 `/upload`+相关性闸门 + 前端 `uploadShop` + 设计文档；真实高德 Key 下三分支（verified/verified_stall/pending）全部正确 |

## 四、需要 Robin 拍板的事项（按优先级）

1. **本轮账号体系提交/推送**：账号体系代码（后端 `auth-server.js`+路由、前端 `login.js`/`auth-client.js`/account 集成/`config`/`app.css`、设计文档 `docs/design/account-auth-*`、`.env` 加 `AUTH_JWT_SECRET`/微信占位、`.env.example`）全部测试通过、未碰红线。建议审查后 commit + push（GitHub origin 已配）。
2. **V4 后端落地（最大决策）**：
   - ① 后端形态——**已按「复用现有接口、自有 Node 单进程 `:8799`」拍板并实施账号子系统**，验证可行；数据(V4.4)同思路待授权。
   - ② 支付/分润密钥托管（env，绝不进前端）——本期未涉及。
   - ③ 本地测试部署已做（非公网），符合红线。
3. **V4.4 数据源统一授权**：是否允许改数据/构建，把 857 vs 625 统一（并修 61 组重名被吞问题）。
4. **高德 Key 安全（建议）**：明文 Key 曾在 git 历史，建议控制台重置；重置后需重建 dist。
5. **真实探店批量升级**：estimated→verified 需你实地采集，工具 `collect-visit.mjs` 已就绪，等手动执行。
6. **真实短信网关接入（生产前置）**：当前 `SMS_PROVIDER=console` 仅开发/演示（控制台打印+返回 devCode）；生产须改 `tencent` 并配 `TENCENT_SMS_SECRET_ID/KEY`，否则 `/auth/sms/send` 如实报「短信服务未配置」。微信登录同理：填 `WECHAT_APPID/SECRET/REDIRECT_URI` 即启用。

## 五、已落地但尚未 push 的改动（本轮）
- 账号登录体系（§7.4.1）全链路：后端 6 端点 + 前端 LoginView/桥接/样式/微信 callback + 设计文档 + 配置。
- 此前已 push 的提交（本日早些时候）：`328e315` §7.4 探店采集+相关性闸门、`0f61f15` 路径修复、`8b7beb8` 入口迁地图页、`6d89966` 永久删除 `docs/archive/`（Robin 授权，git 历史仍可恢复）。

## 六、风险
- 账号后端为内存存储（重启清空），生产需换 Redis/DB（接口已隔离，改动小）。
- `docs/archive/` 已永久删除（git 历史仍可恢复）。
- 未发现已知回归（进程内账号单测 13/13 + vite build 50 模块全绿）。

---

*生成时间：2026-08-13 · 核查方式：直接读取源码 + node 计数 + 测试套件实跑 + git 状态。*
