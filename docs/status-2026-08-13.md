# 蛮有味项目状态总览（2026-08-13 实查）

> 本报告基于实际代码核查（非仅看 roadmap 勾选），由迭代管家生成。

## 一、总体结论
代码地基（数据层 + 算法层 + 前端 + 本地 Agent 运行时）已完整，**单测全绿（h5 13 + hypha 40：upload 22 / agent-client 18）**。**V1–V3 的代码级任务已全部真实落地并测试通过**；**V1 真跑门禁已解锁**（`.env` 已含 `DEEPSEEK_API_KEY`）；**§7.4 探店采集已实施并在真实高德 Key 下三分支验收通过**。当前累积改动停留在工作区未提交（含本轮探店采集 + 相关性闸门修复 + 设计文档）。

## 二、各模块功能完整度

### 1. 数据层（✅ 完成）
- `merchants.js` = 625 商户（自动生成、禁手改）；`all-merchants.js` = 857（含 robin-99 87 + web-stalls 206，坐标全 null 未伪造）。
- 置信度：41 verified + 1 partial + 583 estimated；后端内 **61 组重名被合并去重吞掉**（数据质量隐患，待治理）。

### 2. 算法层（✅ 完成）
- 确定性 `/run`（0 token）+ LLM `/agent` 骨架（:8799）；逐店理由 `explain.js` + 推理时间线。

### 3. 前端 h5（✅ 核心闭环完成）
- 主流程：列表 → 详情 → 领券 → 券包/收藏 → 到店核销 CPS，全通且有测试。
- 信任内核 UI：因子权重可视化、真实性徽章、反广告审计、探店采集工具，均真实实现。
- 增长：校园增长看板 + 文案/二维码占位（域名待定）。
- 地图/榜单/搜索/口味等模块齐全。

### 4. 本地 Agent 运行时 hypha（✅ 完成，真跑未验）
- 10 工具 + 数据源抽象 + DeepSeek 客户端（真实 `fetch` + tool_calling）+ ReAct + httpServer 降级熔断。

## 三、路线图任务完成度

| 版本 | 状态 | 关键说明 |
|---|---|---|
| V1 LLM 基座 | 代码✅ / 真跑🔶待执行 | `deepseek.js`+`httpServer /agent` 已写好并 mock 测试；**`.env` 已含 `DEEPSEEK_API_KEY`，真跑门禁解锁，待在可联网环境实跑 `/agent` 累计成本/延迟** |
| V2 信任内核 | ✅ 全交付 | V2.1–2.4 真实落地 + 测试 |
| V3 增长+账号/券 | ✅ 全交付 | V3.1–3.4 真实落地 + 测试 |
| V4 BFF 后端 | ⛔ 0/4 | 仅 V4.4 只读分析切片完成；`BffStore` 只是"缺 baseUrl 即抛错"的客户端壳，需真实后端 |
| V5 规模化/出海 | ⛔ 未启动 | 依赖 V4 |
| §7.4 探店采集 | ✅ 已实施+真跑验收 | 后端 `/upload`+相关性闸门 + 前端 `uploadShop` + 设计文档；真实高德 Key 下三分支（verified/verified_stall/pending）全部正确 |

## 四、需要 Robin 拍板的事项（按优先级）

1. **提交/推送累积改动（安全、已测试）**：工作区累积改动（原 34 文件 + 本轮 §7.4 探店采集新增/修改：后端 `upload.js`+`httpServer /upload`、前端 `uploadShop.js`+入口+样式、设计文档 `docs/design/shop-upload-*`、单测 `upload.test.mjs`/agent-client 路由断言、`.env` 加 `AMAP_SERVER_KEY`、`.gitignore` 加 `hypha/implementation/data/`），全过测试、未碰红线。建议审查后 `commit`；是否推 GitHub（origin 已配好）由你定。
2. **V1 真跑验收**：在跑 :8799 的环境设 `DEEPSEEK_API_KEY` 后实跑 `/agent` 累计真实成本/延迟 → 满足 V1 完成判定（D-20260813-01 已授权，但需你侧环境）。
3. **V4 后端落地（最大决策）**：
   - ① 后端形态——复用现有 `BffStore`/`BffAuthProvider` 接口自有 Node 扩写，还是另起？
   - ② 支付/分润密钥托管（env，绝不进前端）；
   - ③ 是否允许本地测试部署（非公网）。
4. **V4.4 数据源统一授权**：是否允许改数据/构建，把 857 vs 625 统一（并修 61 组重名被吞问题）。
5. **高德 Key 安全（建议）**：明文 Key 曾在 git 历史，建议控制台重置；重置后需重建 dist。
6. **真实探店批量升级**：estimated→verified 需你实地采集，工具 `collect-visit.mjs` 已就绪，等手动执行。

## 五、风险
- **工作区 34 文件未提交**：放任易冲突/丢失，建议尽快 commit（非必须 push）。
- 高德 Key 历史残留：仅建议项，非紧急。
- 未发现其他已知回归（20 套测试全绿）。

---

*生成时间：2026-08-13 · 核查方式：直接读取源码 + node 计数 + 测试套件实跑 + git 状态。*
