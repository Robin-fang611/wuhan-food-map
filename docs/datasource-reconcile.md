# 数据源口径核对报告（V4.4 基线）

> 自动生成于 2026-08-14T16:06:22.841Z。脚本：`scripts/reconcile-datasource.mjs`（只读分析，不修改数据）。
> 红线：未伪造坐标、未引入 PII/密钥、未改数据文件。

## 1. 总体结论

- **前端聚合层 `allMerchants` = 857 家**（供 detail/ranking/map/account/list 5 视图）。
- **后端 wuhan 数据源 `merchants.js` = 625 家**（BFF 唯一事实源）。
- **前端独有（后端缺失）= 293 家**：来自 `robin-99`(论文致谢强背书) + `web-stalls`(网络公开资料补充)。
- **后端独有（按 id 不在前端）= 61 家**，其中 61 家按店名实则已在前端 → 系后端数据源内**重名被合并去重吞掉**，非真实缺失。
- **后端数据源内重名（归一化店名）共 61 组** → 数据质量待治理（见 §4）。

## 2. 口径差异明细

| 维度 | 前端 allMerchants | 后端 merchants.js | 差 |
|------|------------------|-------------------|----|
| 总商户数 | 857 | 625 | +232 |
| 前端独有 | — | — | 293 |
| 后端独有(含被吞重名) | — | — | 61 |
| 后端内重名组 | — | — | 61 |

### 前端独有来源拆分

- robin-99 论文致谢：87 家
- 网络公开资料(文旅/腾讯地图/大众点评公开页/校周边攻略) 2026-08：206 家

## 3. 分布对比

### 片区（zone）

| zone | 前端 | 后端 |
|------|------|------|
| 武汉全城 | 672 | 525 |
| 财大南湖周边 | 185 | 100 |

### 数据置信度（dataConfidence）

| confidence | 前端 | 后端 |
|------------|------|------|
| estimated | 522 | 583 |
| verified | 41 | 41 |
| partial | 1 | 1 |
| undefined | 293 | 0 |

## 4. 数据质量标记

- 后端 `merchants.js` 内存在 **61 组重名商户**（归一化店名相同），合并进前端时被首条保留、其余静默丢弃。建议在统一口径前先清理（去重/修正店名），属**数据修改**，需 Robin 授权。
- 前端独有 293 家中，坐标为 null 的占比 = 293/293（无伪造坐标，符合红线）。
- 坐标违规计数（外源却带 lng/lat）= 0（应为 0；非 0 即需排查伪造坐标）。

## 5. 统一口径建议（执行需 Robin 授权，本切片不做）

- **方向 A（推荐，对齐前端现状）**：后端 wuhan 数据源也摄入 `robin-99` + `web-stalls`，使后端 = 前端 = 857；Agent 返回 id 已⊂前端集合，零破坏。需把两个外源并入后端构建管线（数据修改 + 构建改动）。
- **方向 B（对齐后端纯净集）**：前端改为仅消费 625 纯净集，前端独有 293 家暂不下发。会削弱前端覆盖（论文致谢/名吃缺失），不推荐。
- **前置清理**：无论 A/B，先治理 §4 的 61 组后端重名，避免合并后计数漂移。
- **红线**：统一过程不得伪造外源坐标、不得引入密钥/PII；真实分润/签约仍走 V4.1–V4.3 BFF（待 Robin 决策）。

---
*本报告由"蛮有味迭代管家"V4.4 安全切片生成，仅分析不改数。*