# 财大校园经济

## 方向（2026-08-03 锁定）

**两个产品，一套后台。**

| 产品 | 目录/资产 | 需求节奏 | 当前角色 |
|------|-----------|----------|----------|
| 新生手册 | `handbook/`（真实内容 16 页 / 约 1.1 万字） | 脉冲，一年约 5 周（9 月开学） | **先发**：引流 + 建社群 |
| 美食地图 | `assets/foodmap-data/`（622 条商户，含经纬度） | 常年在线 | **后发**：社群内传播，未来商家变现 |

- 对外是两个独立入口（各自域名 / 视觉 / 定位干净，不混淆传播）；
- 对内共用一套数据层 + API（前台换壳，后端不重做）。
- `platform/`（12 模块 AI 生成脚手架，无价值）已于 `4507419` 删除，需参考可从 `a83b3f8` 还原。

## 执行顺序

1. **手册优先上线** —— 抓 9 月脉冲窗口，靠学生合伙人 + 已有宣传方法快速扩张、把社群做起来。
2. **社群成型后**，在社群内传播美食地图（常年在线，不赶窗口）。
3. **商家变现后置** —— 等有真实流量与社群后再谈，不硬赶 9 月窗口。

## 关键约束

- 真资产只有两块：`handbook/` 内容 + `assets/foodmap-data/` 数据。任何新建模块必须先有真实内容/数据，不允许空壳页。
- 美食地图数据源 `zuelmap.netlify.app` 仍在线但 `jiangcheng-eats.netlify.app` 已 404，数据已本地存档，勿再依赖远程。
- 旧静态手册内测门禁、高德 Key 等历史遗留见 `.workbuddy/memory/MEMORY.md`。
