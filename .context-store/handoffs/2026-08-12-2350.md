# 交接摘要 2026-08-12-2350

> 真相在源工件（layers/）。本摘要只做"导航 + 快照"，新会话干活读的是 layers/，不是这里。

- 触发原因：用户明确要求开新对话；当前会话已很长（跨多轮数据层补全 + 算法层透明化 + LLM 成本测算 + 架构选型讨论），`analyze_context` 判定活跃上下文 48000 tokens 超预算 8000，为主动接棒。
- 推荐新会话 scope：① push 授权（git commit 0d38e8b 未推）；② LLM 选型落地（多供应商可插拔 vs DeepSeek 付费档实测）；③ estimated 数据升级探店采集。
- 控制塔 tokens：1027（在 <1500 预算内）
- 旧会话：本会话（数据层+算法层+LLM 选型，2026-08-09 ~ 08-12）

## 控制塔快照（交接时）
（见 CONTROL_TOWER.md 全文，已回填。要点：蛮有味 AI 美食发现产品，数据层+算法层已补全测试通过本地可玩；待 Robin 拍板 push 与 LLM 选型；红线=不 push/不部署/不改密钥/不删数据/不付费/不改写历史，输出禁 PII/假坐标/伪造券/密钥，字段避 phone/token/user_id 用 tel。）

## 未结线程（接棒优先处理）
1. **push 授权**：`git commit 0d38e8b`（数据层+算法层，111 文件）已提交本地，工作区干净。未 push（红线需 Robin 授权）。
2. **LLM 选型落地**：待拍板 ① 多供应商可插拔客户端 or ② 先接 DeepSeek 付费档跑真实调用测成本/延迟（D-20260812-01）。
3. **estimated 数据升级**：583 家 estimated 口味/环境为算法推导，待探店采集升级 verified（已标 needsEnrichment）。
4. **前后端数据源口径统一**：后端 wuhan 读 merchants.js(625)，前端用 allMerchants(832)；Agent 返回 id⊂832 不破，统一口径可后续做。
5. **M15 排行榜演进+新玩法 / M16 手册互嵌**：legacy 路线图。
6. **Hypha 3000 共享设施 BLOCKED**：已放弃作主路径（走自有 :8799 Path B），仅记录不投入。
7. **免费档限流风险**：任何免费档不扛真实高并发，规模一来必付费（DeepSeek≈2 分钱/次）。
8. **高德 Key 残余风险**：明文 Key 曾在 git 历史，建议高德控制台重置。

## 接棒指引
1. 运行 `python3 scripts/assemble_context.py --store .context-store --task "<本次任务>" --budget 6000` 重建上下文（含 provenance）。
2. 按 CONTROL_TOWER 与 layers/ 继续；完成后立即回写对应 layers/ 文件与控制塔。
3. 不在本摘要里写新决策——新决策写进 layers/decisions.md（带 D-YYYYMMDD-NN 锚点）。
4. 本会话冗余信息已外置，新会话从库干净接棒，无需重新解释项目背景。
